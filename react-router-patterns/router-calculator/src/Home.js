import React from "react";

const Home = () => (
  <div>
    <h1>React Router Calculator</h1>
    <p>Modify the url path to "/:operation/:num1/:num2"</p>
    <p>Valid operations include add, subtract, multiply, and divide</p>
    <p>num1 and num2 must be numbers</p>
  </div>
);

export default Home;
